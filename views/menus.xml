<!-- In: /opt/odoo-16/custom_addons/transport_management/views/menus.xml -->

<odoo>
    <!-- Top Level Menu -->
    <menuitem
        id="menu_transport_root"
        name="Transport"
        sequence="25"/>

    <!-- Action Menu -->
    <menuitem
        id="menu_transport_mission_action"
        name="Missions"
        parent="menu_transport_root"
        action="action_transport_mission"
        sequence="10"/>

    <!-- Overview Map Menu -->
    <menuitem
        id="menu_transport_mission_overview"
        name="Overview Map"
        parent="menu_transport_root"
        action="action_transport_mission_overview_simple"
        sequence="15"/>

    <!-- KPIs Dashboard Menu -->
    <menuitem
        id="menu_transport_kpi_dashboard"
        name="KPIs"
        parent="menu_transport_root"
        action="action_transport_mission_kpi_dashboard"
        sequence="20"/>

    <!-- Graph quick links under Transport root -->
    <menuitem
        id="menu_transport_mission_graph_cost"
        name="Cost Graph"
        parent="menu_transport_root"
        action="action_transport_mission_graph_cost"
        sequence="21"/>

    <menuitem
        id="menu_transport_mission_graph_distance"
        name="Distance Graph"
        parent="menu_transport_root"
        action="action_transport_mission_graph_distance"
        sequence="22"/>



    <!-- Configuration Submenu -->
    <menuitem
        id="menu_transport_config"
        name="Configuration"
        parent="menu_transport_root"
        sequence="30"/>

    <!-- Cost Parameters Menu -->
    <menuitem
        id="menu_cost_parameters"
        name="Cost Parameters"
        parent="menu_transport_config"
        action="action_transport_cost_parameters"
        sequence="5"/>

    <!-- Route Cache Menu -->
    <menuitem
        id="menu_route_cache"
        name="Route Cache"
        parent="menu_transport_config"
        action="action_route_cache"
        sequence="10"/>

    <!-- Route Cache Actions Menu -->
    <menuitem
        id="menu_route_cache_stats"
        name="Cache Statistics"
        parent="menu_transport_config"
        action="action_route_cache_stats"
        sequence="20"/>

    <menuitem
        id="menu_route_cache_cleanup"
        name="Cleanup Old Routes"
        parent="menu_transport_config"
        action="action_route_cache_cleanup"
        sequence="30"/>

    <!-- Distance Fix Menu -->
    <menuitem
        id="menu_fix_distances"
        name="Fix Mission Distances"
        parent="menu_transport_config"
        action="action_fix_all_distances"
        sequence="40"/>
</odoo>